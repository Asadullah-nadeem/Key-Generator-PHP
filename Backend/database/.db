CREATE TABLE user_data (
  id INT AUTO_INCREMENT PRIMARY KEY,
  name VARCHAR(255),
  phone_number VARCHAR(20),
  age INT,
  address TEXT,
  debit_card_number VARCHAR(20),
  generated_key VARCHAR(64)
);
